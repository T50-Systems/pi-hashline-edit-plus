root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true

# package.json / package-lock.json are rewritten by npm with two-space
# indentation, so JSON is deliberately left unconstrained here.
[*.ts]
indent_style = tab
